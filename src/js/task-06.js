// Напиши скрипт, который при потере фокуса на инпуте (событие blur), проверяет его содержимое на правильное количество введённых символов.

const inputValue = document.querySelector('[data-length]');
const getAtributeLength = inputValue.getAttribute('data-length');


const validationInputValid = document.querySelector('');
const typeofString = typeof '';





// inputValue.addEventListener('blur', onInputValueBlur);

// function onInputValueBlur() {
// typeofString.length === getAtributeLength ?
// }

console.log(validationInputValid)
console.log(getAtributeLength)